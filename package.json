{
  "name": "legaldocs-saas",
  "version": "1.0.0",
  "scripts": {
    "build": "cd legaldocs-frontend && npm install && npm run build",
    "install-backend": "cd legaldocs-backend && rm -rf node_modules package-lock.json && npm install",
    "start": "cd legaldocs-backend && npm start"
  }
}
