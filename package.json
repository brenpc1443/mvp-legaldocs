{
  "name": "legaldocs-saas",
  "version": "1.0.0",
  "scripts": {
    "build": "cd legaldocs-frontend && npm install --legacy-peer-deps --include=dev && npm run build && cd ../legaldocs-backend && npm install --legacy-peer-deps",
    "start": "cd legaldocs-backend && npm start"
  }
}
