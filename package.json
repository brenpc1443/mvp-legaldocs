{
  "name": "legaldocs-saas",
  "version": "1.0.0",
  "scripts": {
    "build": "cd legaldocs-frontend && npm ci --legacy-peer-deps && npm run build",
    "install-backend": "cd legaldocs-backend && npm ci --legacy-peer-deps",
    "start": "cd legaldocs-backend && npm start"
  }
}
