{
  "name": "legaldocs-saas",
  "version": "1.0.0",
  "scripts": {
    "build": "cd legaldocs-frontend && npm install && npm run build && cd ../legaldocs-backend && npm install",
    "start": "cd legaldocs-backend && npm start"
  }
}
